<%= mini_form_for propietario, :as => :propietario, :url => [:crear, :admin, propietario, :archivos], :method => :post do |f| %>
  <%= f.simple_fields_for :adjuntos_attributes, :index => nil do |foto| %>
    <%= foto.input :titulo, :required => true, :placeholder => 'Escriba el título...' %>
    <%= foto.input :archivo, :required => true, :as => :file %>
  <% end %>
  <%= hidden_field_tag :anchor, :adjuntos %>
  <%= f.button :submit, :name => nil, :data => { :disable_with => t('espere') }, :class => 'btn btn-primary' %>
<% end %>